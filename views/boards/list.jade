extends ../layout

block content
	p 
		| All Boards&nbsp;|&nbsp;
		a(href='/w/boards/edit') New Board
	h1 Board List
	ul
		each board, i in boards
			li
				a(href='/w/boards/#{board._id}') #{board.name}
