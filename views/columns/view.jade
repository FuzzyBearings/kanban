extends ../layout

block content
	p
		a(href="/w/boards") All Boards
		|&nbsp;|&nbsp;
		a(href="/w/boards/0") New Board
		if board._id
			|&nbsp;|&nbsp;
			a(href='/w/boards/#{board._id}') View Board

	h1 Column View

	ul
		li #{column.name}

	h1 Stories
